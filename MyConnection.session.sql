CREATE DATABASE IF NOT EXISTS college;

CREATE table MCA1(id int primary key,name varchar(50),branch varchar(40),semester varchar(20));

CREATE TABLE student(id int primary key,name varchar(40),age int,class varchar(40));

SHOW table;

drop DATABASE college;